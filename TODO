-> avoid hysteresis (limit the frequency of state updates (example 15 minutes))
-> avoid updating soc with crappy readings (more scrutine reading validaiton).
-> kalman filter measurement step (try it with arrays)
-> fix data logger readout bug
-> fix the time flow issue
-> add some documentation about
    a) user interface
    b) code organization

